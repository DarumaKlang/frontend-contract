// lib/admin.ts – placeholder implementation for admin requirement
// This stub simply allows all requests (no admin check). Replace with real logic as needed.
export async function requireAdmin(_req: Request): Promise<null> {
  // Return null to indicate no error; the route can continue.
  return null;
}
