# Supabase setup

Run the SQL in [migrations/001_create_contracts.sql](migrations/001_create_contracts.sql) in your Supabase SQL Editor.

It creates the contracts table used by the app and enables row-level security so each authenticated user can only access their own records.
